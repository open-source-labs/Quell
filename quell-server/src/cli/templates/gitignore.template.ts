export const gitignoreAdditions = `
# Quell cache configuration
.env
.env.local
.env.development
.env.production

# Redis dump file
dump.rdb

# Node modules (if not already present)
node_modules/
npm-debug.log
yarn-error.log
`;
