const buildFromCache = require('../../src/helpers/buildFromCache');

xdescribe('buildFromCache.test.js', () => {
  test.skip('buildFromCache.test.js', () => {
    expect(buildFromCache()).toEqual({});
  });
});
