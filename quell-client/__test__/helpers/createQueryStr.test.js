const createQueryStr = require('../../helpers/createQueryStr');

describe('createQueryStr', () => {
  test.only('should return an array of strings and numbers', () => {
    expect(createQueryStr()).toEqual();
    expect(createQueryStr()).toEqual();
  });
});
