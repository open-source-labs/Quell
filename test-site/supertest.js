const request = require('supertest');
const app = require('../server/server');

// const query = {
//   "query": "{ countries: {id name capital }}"
// }

describe('Route integration', () => {
  // describe('/graphql', () => {
  //   describe('POST', () => {
  //     it('responds with 200 status on valid request', () => {
  //       return request(app)
  //         // .post('/graphql')
  //         // .send(query)
  //         // .expect(200)
  //         // .then(() => done())
  //     });
  //   });
    
    // describe('POST', () => {
    //   it('responds with 400 status on invalid request', () => {
    //     return request(app)
    //       .post('/graphql')
    //       .send('errr')
    //       .expect(400)
    //   });
    // });
  // });
});